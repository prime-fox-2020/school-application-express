<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>School App (students)</title>
</head>
<body>
    <h1>Hai Gaes! Welcome back to our website page</h1>
    <p>this is Home page from views 2</p>
    <pre><%= colors %></pre>
    <ul>
        <% for(let i = 0; i < colors.length; i++) {%>
            <% if(colors[i].length % 2 == 0) {%>
                <li><%= colors[i] %></li>
            <%} %>
        <%} %>
    </ul>
</body>
</html>
