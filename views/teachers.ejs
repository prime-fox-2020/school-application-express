<!DOCTYPE html>
<head>
    <title>Teacher's List</title>
</head>

<body>
    <h1> Teachers's List</h1>
        <table border = 1 >
        <tr>
            <th>Id</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>E-mail</th>
        </tr>
        <% for(var i = 0; i< teachersParse.length; i++) {%>
        <tr>
            <td><%= teachersParse[i].id %></td>
            <td><%= teachersParse[i].first_name %></td>
            <td><%= teachersParse[i].last_name %></td>
            <td><%= teachersParse[i].email %></td>
        </tr>

        <% } %>

    </table>
</body>